Nodejs and Angular installation in ubuntu

sudo apt update
sudo apt install nodejs
node -v
sudo apt install npm
sudo npm install -y @angular/cli
sudo npm
npm cache clean -f
sudo npm install -g n
sudo n latest
ng version
sudo ng new (file name)

To run the program:
ng serve